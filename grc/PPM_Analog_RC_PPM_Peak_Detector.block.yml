# auto-generated by grc.converter

id: PPM_Analog_RC_PPM_Peak_Detector
label: PPM Peak Detector
category: '[PPM_ANALOG_RC]'

parameters:
-   id: peak_threshold
    label: Peak Threshold
    dtype: float
    default: '0.0'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.PPM_Analog_RC as PPM_Analog_RC
    make: PPM_Analog_RC.PPM_Peak_Detector(${peak_threshold})

file_format: 1
